''' Test MODULE.bazel file to see how it works with C Projects '''

module(
  name = "shared_lib",
  version = "1.0.3",
  compatibility_level = 1,
)

bazel_dep(
  name = "compilers",
  version = "1.0.2",
)

bazel_dep(
  name = "platforms",
  version = "0.0.4",
)
